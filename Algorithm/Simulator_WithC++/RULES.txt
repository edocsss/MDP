Python to C++:
- Send Robot's information --> X, Y, Orientation, MapKnowledge
- How to transfer MapKnowledge??
	- In ArenaMap --> create one method to convert to String of 300 numbers

import subprocess
import os
output = subprocess.Popen([fileName, args**], stdout=subprocess.PIPE).communicate()[0]

C++ to Python:
- List of Actions --> use String?
	- '0' = F
	- '1' = L
	- '2' = R
- In Python, use .decode().split('\r\n')
